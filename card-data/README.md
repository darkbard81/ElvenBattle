# Card Data

이 디렉터리는 독자 카드 정의 JSON을 보관한다.

원작 카드 데이터, 명칭, 일러스트, 스토리, 고유 리소스를 복제하지 않는다.

카드 JSON은 룰 처리의 원본 데이터다. Phase 9 렌더러는 이 데이터를 표시 모델로
정규화하지만, 이미지에 합성된 값이나 텍스트를 룰 판정에 사용하지 않는다.

`nameKey`와 `abilities[].textKey`는 표시 문자열 조회용 key다. 표시 문자열이 없는 개발
단계에서는 key 자체를 fallback으로 사용할 수 있다.
